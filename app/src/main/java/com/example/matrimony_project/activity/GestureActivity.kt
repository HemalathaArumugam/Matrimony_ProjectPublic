
    package com.example.matrimony_project.activity

    import android.os.Bundle
    import androidx.appcompat.app.AppCompatActivity
    import androidx.databinding.DataBindingUtil
    import androidx.lifecycle.ViewModelProvider
    import com.example.matrimony_project.R
   // import com.example.matrimony_project.adapter.GestureAdapter
    import com.example.matrimony_project.databinding.ActivityGestureBinding
    //import com.example.matrimony_project.livedata.GestureLiveModel


    class GestureActivity : AppCompatActivity() {

     /*   private lateinit var binding: ActivityGestureBinding
        private lateinit var adapter: GestureAdapter
        private lateinit var viewModel: GestureLiveModel


        override fun onCreate(savedInstanceState: Bundle?) {
            super.onCreate(savedInstanceState)

            binding = DataBindingUtil.setContentView(this, R.layout.activity_gesture)
            viewModel = ViewModelProvider(this).get(GestureLiveModel::class.java)
            binding.gesture = viewModel
            binding.lifecycleOwner = this

            recyclerview()
            viewModel.gestureProfiles.observe(this) { profiles ->
                adapter.updateProfiles(profiles)
            }
        }

        private fun recyclerview() {
            adapter = GestureAdapter(emptyList())
            binding.stackView.adapter = adapter
        }*/

    }

